extends admin_base

block content
  .container
    .panel.panel-default
      .panel-heading #{dbName}
      table.table.table-striped
        - for (var i = 0; i < collections.length; ++i) {
          tr 
            td
              a(href='/optadmin/collection/#{collections[i]}')=collections[i]
        - }
        
          
    